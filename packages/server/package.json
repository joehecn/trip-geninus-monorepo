{
  "name": "trip-geninus-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:format": "npx dotenv-cli npx prisma format",
    "prisma:generate": "npx dotenv-cli npx prisma generate",
    "prisma:validate": "npx dotenv-cli npx prisma validate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.15.0"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "prisma": "^6.15.0"
  }
}